package org.generation.repository;

// import org.generation.model.Employee;

// Las interfaces son contratos que las clases deben cumplir. 
// Las interfaces reciben métodos abstractos, es decir, métodos con firma (head) pero sin cuerpo.
// Las interfaces deben implementarse dentro de las clases. Se debe anotar los métodos que se implementan desde la interface con la anotación @Override
// --- Implementar interfaces cumple con el pilar de la ABSTRACCIÓN de la POO
public interface EmployeeRepository {
	// Métodos abstractos que se pueden implementar en cualquier Clase
	// public void calcularSalario(Employee employee);
}
